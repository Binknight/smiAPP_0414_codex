任务内容：

你当前应在 `C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\apps\scenarios\travel\scenario001` 目录下工作，基于对应基线 APP（来源 `apps/baseline/travelApp`）的副本完成场景还原。

场景基础信息：
- 场景输入 JSON 路径：C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\scenario1.json
- 场景输入 JSON 内容：
```json
{
  "app": "travel",
  "question": "在途屿旅行 APP 中添加一个特价机票推荐模块"
}
```
- APP 类型：travel
- APP 类型说明：出行类APP
- 基线来源目录：apps/baseline/travelApp
- 当前场景目录：C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\apps\scenarios\travel\scenario001
- 场景名称：scenario001
- spec 输出目录：C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\apps\scenarios\travel\scenario001\spec
- mock 数据目录：C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\apps\scenarios\travel\scenario001\mock-data
- 结果 JSON 输出路径：C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\apps\scenarios\travel\scenario001\output\result.json
- 构建目标：`apps/scenarios/travel/scenario001`
- 构建命令：`powershell -ExecutionPolicy Bypass -File .\build\build.ps1 -Target apps/scenarios/travel/scenario001`

任务要求：

1. 优先使用名为 `simapp-scenario-spec-gen` 的 skill 生成 APP 开发 spec，并将 spec 文件保存到 `C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\apps\scenarios\travel\scenario001\spec`。如果当前运行环境无法发现或调用该 skill，必须直接读取并遵循仓库文件 `skills/simapp-scenario-spec-gen/SKILL.md` 中的完整规则后继续执行，不能因为“找不到 skill”而跳过 spec 生成。
2. 基于 app spec 制定仿真 APP 开发计划，并按计划直接实施开发。
3. 所有相关数据源采用 mock 方式，拆分为各自独立的 JSON 文件，并统一放在 `C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\apps\scenarios\travel\scenario001\mock-data` 目录下。
4. 开发任务完成后，严格按照 `README.md` 的编译构建指导执行出包；构建目标必须使用 `apps/scenarios/travel/scenario001`。
5. 如果出包成功，请将结果 JSON 写入 `C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\apps\scenarios\travel\scenario001\output\result.json`，至少包含以下字段：
   - `buildStatus`
   - `artifactPath`
   - `artifactSizeBytes`
   - `buildTime`
   - `scenarioDir`
   - `appType`
6. 如果构建失败，也必须输出结果 JSON，写明失败状态、失败时间、错误摘要和最后一次可用日志路径。

执行约束：

- 你只能修改当前场景目录 `C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\apps\scenarios\travel\scenario001` 下的文件与目录；除此之外，不允许修改仓库中的任何其他路径。
- 允许写入的结果文件仅限 `C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\apps\scenarios\travel\scenario001\output\result.json`，且该文件必须位于当前场景目录内。
- 严禁修改以下路径或其中任何文件：`apps/baseline/`、`dev/`、`build/`、`README.md`、仓库根目录下除当前场景目录外的其他内容。
- 如果你判断完成任务必须修改 `C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\apps\scenarios\travel\scenario001` 之外的文件，必须立即停止继续实现，在结果 JSON 中明确写出原因，并将任务标记为失败；不要擅自越界修改。
- 优先做增量修改，避免无关重构；禁止对基础框架、公共配置、构建脚本和基线工程做顺手优化或清理。
- 不要跳过构建验证。
- 不要把 mock 数据内联在代码里，必须落成独立 JSON 文件。
- 关键过程请输出清晰日志，便于命令行追踪。
- 在结束前必须自检本次修改范围；如果发现 `C:\Users\ttelab\commApp\simApp-main\smiAPP_0414_codex\apps\scenarios\travel\scenario001` 之外存在任何新增、删除或修改，视为任务失败，并在结果 JSON 和日志中明确列出越界路径。
