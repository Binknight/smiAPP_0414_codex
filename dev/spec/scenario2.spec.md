# SimApp 特定场景增量开发 Spec

## 1. Spec 元信息

- 场景来源：`C:\Users\ttelab\commApp\scenario2.json`
- 输出文件：`dev/spec/scenario2.spec.md`
- 目标 App / 业务域：`general` / generic 通用类 APP
- 仓库基线：`main`
- Spec 范围：针对“什么都不需要实现”场景生成可交付 spec、整理 mock 数据、将示例首页收敛为可验收的空场景说明页，并完成构建验证
- 置信度：`medium confidence`

## 2. 场景摘要

### 2.1 原始问题描述

- `question`：什么都不需要实现
- `exception`：空
- `exception_type`：空
- `exception_reason`：空
- `app`：`general`
- `flow`：空
- `flow_reason`：空

### 2.2 对应特定场景的摘要

该场景没有提出任何新增业务能力、异常修复或流程还原要求。结合当前分支的 HarmonyOS ArkUI 示例工程基线，最合理的实现范围是：

- 输出结构化场景 spec
- 将场景说明与交付元数据拆分为独立 mock JSON 文件
- 将首页从通用 demo 计数器/待办清单改成“当前场景无需新增实现”的说明页
- 通过完整构建验证交付物可正常出包

### 2.3 预期行为

- 应用启动后进入单一首页
- 首页明确展示该场景没有功能开发项，仅保留可构建的通用壳页面
- 仓库内存在可维护的 mock 数据与 spec 文件
- 构建脚本执行成功并产出 HAP

## 3. 证据清单

### 3.1 JSON 证据

- 已确认唯一需求：`question = 什么都不需要实现`
- 未提供异常、流程、截图、UTG 或业务补充信息
- `app = general`，说明这是通用类场景，不指向任何垂直业务模块

### 3.2 截图证据

- 未提供截图证据

### 3.3 UTG 证据

- 未提供 UTG 证据

### 3.4 信息缺口与歧义点

- 未指定目标页面样式、模块名、展示文案细节
- 未提供需要保留或去除的原 demo 交互说明
- 未要求接入真实数据或仿真业务流程
- 因缺少截图和 UTG，无法支持更细粒度的 UI 还原

## 4. 重建后的产品流程

### 4.1 前置条件

- 用户安装并启动应用
- 仓库中的 mock 数据和 spec 已生成

### 4.2 页面序列

1. `EntryAbility`
2. `pages/Index`

### 4.3 用户动作与系统响应

1. 用户启动应用
2. 系统加载 `pages/Index`
3. 页面展示场景说明、当前交付范围与验收提示

### 4.4 重点关注的触发点

- 首屏加载成功
- 页面文案与场景语义一致
- 不再展示与场景无关的 demo 计数器和待办交互

## 5. UI / 页面设计规格

### 5.1 涉及页面

- `pages/Index`：场景说明首页

### 5.2 页面级布局与分区

- 顶部标题区：展示场景编号和核心结论
- 主状态卡片：说明“无需新增实现”
- 交付说明区：展示本次交付包含 spec、mock 数据和构建验证
- 验收提示区：列出页面应满足的静态展示要求

### 5.3 关键 UI 组件与状态

- 标题文本
- 说明文本
- 多个静态信息卡片
- 静态列表项
- 页面仅使用本地静态展示状态，不引入业务交互

### 5.4 导航与路由规则

- 保持单页入口
- 不新增页面注册
- 不新增业务路由

### 5.5 状态与数据要求

- 页面展示文案对应的维护数据应拆分为独立 mock JSON
- 不接入真实网络请求
- 不要求持久化
- 不新增业务状态机

### 5.6 基于截图 / UTG 的 UI 精细化建模

`reserved / not implemented in this pass`

## 6. 框架差距分析

### 6.1 现有框架能力

- 已具备单页 ArkUI 布局能力
- 已具备基础文本、按钮、列表组件能力
- 已具备构建脚本与 HAP 打包能力

### 6.2 已实现需求

- 单页应用壳已存在
- 构建链路已存在

### 6.3 需要修改现有代码的需求

- 将现有 `Index.ets` 的 demo 文案和交互改成与空场景一致的静态说明页

### 6.4 需要新增实现的能力

- 生成场景 spec 文件
- 新增独立 mock JSON 文件用于维护场景说明和交付元信息

### 6.5 因场景信息缺失导致的阻塞项

- 无业务阻塞项
- 由于需求明确为“无需实现”，不应额外扩展业务功能

## 7. 增量开发计划

### 7.1 可能变更的文件或模块

- `dev/spec/scenario2.spec.md`
- `dev/mock-data/generic-002/*.json`
- `entry/src/main/ets/pages/Index.ets`

### 7.2 实现任务

1. 依据场景 JSON 和 skill 模板生成 spec，并落盘到 `dev/spec`
2. 在 `dev/mock-data/generic-002` 下拆分场景元数据、页面摘要、验收项 JSON
3. 将首页改造成静态说明页，移除与场景无关的 demo 交互
4. 执行 `build.ps1`，确认 HAP 正常产出
5. 根据构建结果写入 `dev/output/generic-002.result.json`

### 7.3 非目标项

- 不新增网络请求
- 不新增多页面流程
- 不恢复不存在的业务模块
- 不实现任何真实业务逻辑

## 8. 测试策略

### 8.1 功能测试用例

- 应用启动后成功进入首页
- 首页标题和说明文案反映“无需新增实现”的场景语义
- 页面中不再出现旧版 demo 计数器和待办清单

### 8.2 UI / 交互验证

- 页面分区清晰，适配手机和平板默认布局
- 卡片和列表可正常渲染，无空白崩溃

### 8.3 回归覆盖范围

- `EntryAbility` 启动加载首页
- ArkUI 页面编译通过
- HAP 产物生成成功

## 9. 验收标准

- 标准 1：`dev/spec/scenario2.spec.md` 已生成且内容与场景一致
- 标准 2：`dev/mock-data/generic-002` 下已存在独立 JSON mock 文件
- 标准 3：构建成功并生成结果 JSON

## 10. 假设与开放问题

- 假设当前场景只要求交付可构建的通用说明页，而不是保留原 demo 交互
- 假设 mock JSON 在本次场景中主要用于维护交付说明，不需要驱动复杂运行时流程
- 如后续希望页面在运行时直接读取 `dev/mock-data`，需要额外引入资源同步或原始文件读取方案
