任务内容：

你当前所在仓库已经切换到目标 APP 场景分支，请直接在当前工作区内完成以下任务，不要只输出分析结论，必须落盘实现并完成构建验证。

场景基础信息：
- 场景输入 JSON 路径：{{INPUT_JSON_PATH}}
- 场景输入 JSON 内容：
```json
{{INPUT_JSON_CONTENT}}
```
- APP 类型：{{APP_TYPE}}
- APP 类型说明：{{APP_DISPLAY_NAME}}
- 基础分支：{{BASE_BRANCH}}
- 当前开发分支：{{SCENARIO_BRANCH}}
- mock 数据目录：{{MOCK_DATA_DIR}}
- 结果 JSON 输出路径：{{RESULT_JSON_PATH}}
- 构建命令：`{{BUILD_COMMAND}}`

任务要求：

1. 调用 `simapp-scenario-spec-gen` skill 生成 APP 开发 spec，并将 spec 文件保存到仓库内。
2. 基于 app spec 制定仿真 APP 开发计划，并按计划直接实施开发。
3. 所有相关数据源采用 mock 方式，拆分为各自独立的 JSON 文件，并统一放在 `{{MOCK_DATA_DIR}}` 目录下，便于后期维护。
4. 开发任务完成后，严格按照 `README.md` 的编译构建指导执行出包；过程中遇到的错误必须继续排查并修复，直到编译构建成功才算完成。
5. 如果出包成功，请将结果 JSON 写入 `{{RESULT_JSON_PATH}}`，至少包含以下字段：
   - `buildStatus`
   - `artifactPath`
   - `artifactSizeBytes`
   - `buildTime`
   - `scenarioBranch`
   - `appType`
6. 如果构建失败，也必须输出结果 JSON，写明失败状态、失败时间、错误摘要和最后一次可用日志路径，便于自动巡检和恢复。

执行约束：

- 优先做增量修改，避免无关重构。
- 不要跳过构建验证。
- 不要把 mock 数据内联在代码里，必须落成独立 JSON 文件。
- 关键过程请输出清晰日志，便于命令行追踪。
