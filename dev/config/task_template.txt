任务内容：

你当前所在仓库已经切换到目标 APP 基线分支，请直接在当前工作区内完成以下任务，不要只输出分析结论，必须落盘实现并完成构建验证。

场景基础信息：
- 场景输入 JSON 路径：{{INPUT_JSON_PATH}}
- 场景输入 JSON 内容：
```json
{{INPUT_JSON_CONTENT}}
```
- APP 类型：{{APP_TYPE}}
- APP 类型说明：{{APP_DISPLAY_NAME}}
- 当前基线分支：{{BASE_BRANCH}}
- 当前场景目录：{{SCENARIO_ROOT}}
- 场景名称：{{SCENARIO_NAME}}
- spec 输出目录：{{SPEC_DIR}}
- mock 数据目录：{{MOCK_DATA_DIR}}
- 结果 JSON 输出路径：{{RESULT_JSON_PATH}}
- 构建目标：`{{BUILD_TARGET}}`
- 构建命令：`{{BUILD_COMMAND}}`

任务要求：

1. 优先使用名为 `simapp-scenario-spec-gen` 的 skill 生成 APP 开发 spec，并将 spec 文件保存到 `{{SPEC_DIR}}`。如果当前运行环境无法发现或调用该 skill，必须直接读取并遵循仓库文件 `skills/simapp-scenario-spec-gen/SKILL.md` 中的完整规则后继续执行，不能因为“找不到 skill”而跳过 spec 生成。
2. 基于 app spec 制定仿真 APP 开发计划，并按计划直接实施开发。
3. 所有相关数据源采用 mock 方式，拆分为各自独立的 JSON 文件，并统一放在 `{{MOCK_DATA_DIR}}` 目录下。
4. 开发任务完成后，严格按照 `README.md` 的编译构建指导执行出包；构建目标必须使用 `{{BUILD_TARGET}}`。
5. 如果出包成功，请将结果 JSON 写入 `{{RESULT_JSON_PATH}}`，至少包含以下字段：
   - `buildStatus`
   - `artifactPath`
   - `artifactSizeBytes`
   - `buildTime`
   - `scenarioBranch`
   - `appType`
6. 如果构建失败，也必须输出结果 JSON，写明失败状态、失败时间、错误摘要和最后一次可用日志路径。

执行约束：

- 你只能修改当前场景目录 `{{SCENARIO_ROOT}}` 下的文件与目录；除此之外，不允许修改仓库中的任何其他路径。
- 允许写入的结果文件仅限 `{{RESULT_JSON_PATH}}`，且该文件必须位于当前场景目录内。
- 严禁修改以下路径或其中任何文件：`baseApp/`、`dev/`、`build/`、`README.md`、仓库根目录下除当前场景目录外的其他内容。
- 如果你判断完成任务必须修改 `{{SCENARIO_ROOT}}` 之外的文件，必须立即停止继续实现，在结果 JSON 中明确写出原因，并将任务标记为失败；不要擅自越界修改。
- 优先做增量修改，避免无关重构；禁止对基础框架、公共配置、构建脚本和基线工程做顺手优化或清理。
- 不要跳过构建验证。
- 不要把 mock 数据内联在代码里，必须落成独立 JSON 文件。
- 关键过程请输出清晰日志，便于命令行追踪。
- 在结束前必须自检本次修改范围；如果发现 `{{SCENARIO_ROOT}}` 之外存在任何新增、删除或修改，视为任务失败，并在结果 JSON 和日志中明确列出越界路径。
