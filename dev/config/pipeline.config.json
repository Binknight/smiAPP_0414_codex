{
  "paths": {
    "repo_root": ".",
    "default_input_json": "scenario1.json",
    "task_template": "dev/config/task_template.txt",
    "baseline_root": "apps/baseline",
    "scenarios_root": "apps/scenarios",
    "build_root": "build"
  },
  "app_types": {
    "generic": {
      "display_name": "通用类APP",
      "aliases": ["generic", "common", "main", "general"],
      "baseline_dir": "apps/baseline/commonApp"
    },
    "explore": {
      "display_name": "内容类APP",
      "aliases": ["explore", "content"],
      "baseline_dir": "apps/baseline/exploreApp"
    },
    "travel": {
      "display_name": "出行类APP",
      "aliases": ["travel", "trip", "tour"],
      "baseline_dir": "apps/baseline/travelApp"
    },
    "shopping": {
      "display_name": "购物类APP",
      "aliases": ["shopping", "shop", "ecom", "mall", "retail", "store", "buy"],
      "baseline_dir": "apps/baseline/shoppingApp"
    },
    "delivery": {
      "display_name": "外卖类APP",
      "aliases": ["delivery", "waimai", "food-delivery", "takeout", "takeaway", "food"],
      "baseline_dir": "apps/baseline/deliveryApp"
    }
  },
  "git": {
    "remote_name": "origin",
    "scenario_id_padding": 3
  },
  "agent": {
    "active": "opencode_cli",
    "definitions": {
      "codex_cli": {
        "display_name": "Codex CLI",
        "command": ["codex.cmd", "exec", "--yolo", "-"],
        "env": {},
        "task_via_stdin": true,
        "runtime_family": "codex",
        "supports_resume": false
      },
      "opencode_cli": {
        "display_name": "OpenCode CLI",
        "command": [
          "opencode.cmd",
          "run",
          "--model",
          "{{OPENCODE_MODEL}}",
          "请阅读并完整执行 -f 附加文件中的任务说明。",
          "-f",
          "{{TASK_PROMPT_FILE}}"
        ],
        "env": {},
        "task_via_stdin": false,
        "runtime_family": "opencode",
        "supports_resume": false
      }
    }
  },
  "build": {
    "command": "powershell -ExecutionPolicy Bypass -File .\\build\\build.ps1",
    "readme_reference": "README.md"
  },
  "scheduler": {
    "poll_interval_seconds": 60,
    "max_cycles": 60,
    "success_values": ["success", "succeeded", "ok", "passed", "true"],
    "commit_message_template": "feat: automate {pipeline_key}"
  },
  "commit": {
    "shared_include_paths": []
  }
}
